{
  "name": "transom",
  "version": "0.1.0",
  "private": true,
  "description": "A standalone Cloudflare Worker that answers a Twilio voice line (signed webhooks, IVR, voicemail, forwarding) and sits as an edge layer in front of a third-party app.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail --format pretty"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260919.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.90.0"
  }
}
